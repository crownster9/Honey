const products = [['Wildflower','Bright & floral','$18'],['Clover','Soft & buttery','$16'],['Forest','Deep & caramel','$20']]
export default function Gallery({ onAddToCart }) { return <section className="section" id="gallery"><p className="eyebrow">From our hives</p><h2>Find your favourite golden thing.</h2><div className="products">{products.map(([name,note,price], index) => <article className={`product p${index}`} key={name}><div className="mini-jar">HONEY<small>{name}</small></div><div><h3>{name}</h3><p>{note}</p></div><button type="button" onClick={onAddToCart}>Add · {price}</button></article>)}</div></section> }
